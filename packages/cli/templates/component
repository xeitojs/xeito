import { XeitoComponent, Component, html } from '@xeito/core';
import styles from './{{stylesPath}}.module.scss';

@Component({
  selector: '{{selectorName}}',
})
export class {{className}} extends XeitoComponent {

  onDidMount() {
    console.log('{{className}} works!');
  }

  render() {
    return html`
      <div>
        <p>{{className}} works!</p>
      </div>
    `;
  }

}
